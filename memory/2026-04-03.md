# 2026-04-03

- `pnpm stop` cleanly shut down the repo-managed DroidAgent instances; no repo server/web/e2e listeners remained afterward. A separate system `ollama` listener was still present on `127.0.0.1:11434`.
- Current deterministic perf artifacts still show route switching as the main client-side budget risk: `route_switch_ms` was `198.30 ms` on chromium and `191.95 ms` on fold-mobile, which passes the checked-in `220 ms` gate but misses the stricter `180 ms` stretch target from the recent hardening plan.
- Chat transcript rendering is still full-list rendering in `apps/web/src/components/chat-screen-shell.tsx`; the transcript maps every message and the controller still performs bottom-anchor scrolling against full `scrollHeight`, so long-history smoothness work is still outstanding.
- `dashboard.snapshot` server diagnostics remain too synthetic to represent cold dashboard cost accurately; the latest server artifact recorded `dashboard.snapshot` samples at `0 ms` while the first real `GET /api/dashboard` request in the same run took about `910 ms`.
- The server still has broad `openclawService` coupling outside the harness boundary. `websocket-hub.ts`, `operations-routes.ts`, `dashboard-service.ts`, and several services still call the large `openclawService` surface directly.
- Current perf budgets do not yet guard the largest shared client chunks. The build gates only track the main entry and terminal route while the generated bundle still carries large shared assets such as the app-shell, React vendor, markdown, and xterm chunks.
- The follow-up hardening pass replaced the misleading cold dashboard gauge with first-request server artifacts, stamped `dashboard.snapshot` samples with cache context, and split internal compose timing into `dashboard.snapshot.compose`.
- Shared perf budgets now cover `app-shell`, `react-vendor`, `router-vendor`, `react-markdown`, and `xterm` instead of only the main entry and terminal leaf chunk.
- The websocket reconnect path now reconnects transport immediately when the browser comes back online and resyncs dashboard/access in the background; the validated `2026-04-03` perf run recorded `reconnect_resync_ms` at `18.97 ms` on chromium and `61.67 ms` on fold-mobile.
- The `Files` route is now kept hot in the shell. The validated `2026-04-03` perf run still leaves route switching as the main remaining stretch-target gap at `195.46 ms` on chromium and `189.22 ms` on fold-mobile, which passes the checked-in `220 ms` budget but not the stricter `180 ms` plan target.
- The follow-up route-tightening pass keeps the `Files` surface mounted in the shell and measures `route_switch_ms` from in-browser route activation to the first visible destination control instead of from Playwright's outer click path.
- The fully validated `2026-04-03` perf gate after that route pass recorded `route_switch_ms` at `8.10 ms`, `reconnect_resync_ms` at `23.15 ms`, `chat_first_token_visible_ms` at `110.67 ms`, server `GET /api/access` p95 at `2.39 ms`, and server `GET /api/dashboard` p95 at `2.37 ms`; `pnpm perf:check` passed with the stricter `180 ms` route-switch budget on `main`.
- The security remediation pass replaced the old OpenClaw tarball pin with the patched npm release `2026.3.31` and pinned the nested `@anthropic-ai/sdk` dependency to `0.82.0`; `pnpm audit --prod` is clean on the repo after that update.
- GitHub's follow-up lockfile scan also surfaced transitive `lodash` alerts through `workbox-build`, so the root overrides now pin `lodash` to the patched `4.18.1` release as well.
- `dashboard-service.ts`, `websocket-hub.ts`, and `http/operations-routes.ts` now import focused OpenClaw facets from `apps/server/src/services/openclaw-service-facets.ts` instead of depending directly on the full `openclawService` surface for memory/context/gateway status work.
- The next server-boundary pass extended those facets across `quickstart-service.ts`, `memory-prepare-service.ts`, `memory-draft-service.ts`, `startup-service.ts`, `runtime-service.ts`, `maintenance-service.ts`, `file-service.ts`, `signal-service.ts`, `decision-service.ts`, and `index.ts`, leaving direct `openclawService` usage isolated to the harness layer plus `openclaw-service-facets.ts`.
- The secure dependency tree kept all absolute perf budgets green but nudged a few tiny E2E timings above the old regression snapshot, so `artifacts/perf/baseline.json` was refreshed on `2026-04-03`; the refreshed deterministic run recorded `route_switch_ms` p95 at `9.10 ms`, `reconnect_resync_ms` at `25.08 ms`, `chat_first_token_visible_ms` at `54.00 ms`, server `GET /api/access` p95 at `1.97 ms`, and server `GET /api/dashboard` p95 at `2.18 ms`.
- The validated boundary pass on the same day kept absolute perf budgets green again; a fresh local baseline was refreshed after the run, with server `GET /api/access` p95 at `1.73 ms`, server `GET /api/dashboard` p95 at `1.74 ms`, first authenticated `GET /api/dashboard` at `3.37 ms`, `route_switch_ms` p95 at `9.90 ms`, and `chat_first_token_visible_ms` p95 at `55.45 ms`.
- The local Ollama profile path is now canonicalized through `applyOllamaProfile` plus `runtimeService.configureOllamaProfile`, so model selection, context-window updates, quickstart handoff, and benchmark setup all reuse the same runtime/harness/update path instead of duplicating local-model wiring.
- The live perf lane now boots a seeded authenticated server without `DROIDAGENT_TEST_MODE`, applies the requested Ollama profile through `/api/runtime/ollama/profile`, records the active model/context in perf artifacts, and writes live artifacts to `artifacts/perf/live/current/` instead of clobbering deterministic perf output.
- The maintained model comparison lane is now `pnpm perf:model-compare`, currently comparing `qwen3.5:4b` vs `gemma4:e4b` at `65536`, with isolated per-profile artifacts under `artifacts/perf/model-compare/` and a summary report in `compare-summary.json`.
- The server hot chat path now uses the shared OpenClaw gateway websocket client for `chat.send` and `chat.abort` instead of the broken HTTP `/v1/chat/completions` bridge, with ordered delta handling, tool-state relay, and non-fatal local abort handling.
- Live runtime benchmarking now supports provider-specific profiles through the same runtime routes the product uses. The e2e/perf harness applies either Ollama or llama.cpp profiles, and each perf lane now gets its own isolated app/OpenClaw ports so stale live servers cannot poison a later comparison run.
- `runtimeService.configureLlamaCppProfile` plus `POST /api/runtime/llamacpp/profile` are now the canonical llama.cpp profile path, the maintained Gemma 4 E4B GGUF preset is registered there, and Hugging Face `resolve/main/*.gguf` URLs normalize to `repo:Q4_K_M` automatically.
- Ollama on this machine is now running the `0.20.0` backend, `gemma4:e4b` is installed locally, and `ollama show gemma4:e4b` reports `completion`, `vision`, `audio`, `tools`, and `thinking` with a native `131072` context limit and `Q4_K_M` quantization.
- The maintained benchmark/docs line is back on the direct Ollama candidate: `qwen3.5:4b` baseline vs `gemma4:e4b` at `65536`. The optional llama.cpp fallback lane remains available as `gemma4_e4b_hf_65k`.
- Vision-capable local primary models now keep multimodal attachment routing on the same provider path for both Ollama and llama.cpp. Gemma-family llama.cpp repos are now marked `text + image`, and image/PDF defaults stay on the local primary model instead of always forcing the Ollama fallback.
