# 2026-04-02

- Added a shared server-side realtime mutation queue to coalesce dashboard invalidation and slice fanout, with bounded patch flushing still preserved for chat/job/terminal streams.
- Canonicalized client chat session state in `apps/web/src/lib/chat-session-store.ts`; chat history, optimistic sends, run state, streaming text, feedback timing, and session switching now flow through one store instead of separate run/stream/feedback layers.
- `use-websocket.ts` is the single websocket transport consumer for chat and terminal events; `app-context.tsx` no longer double-applies terminal websocket updates.
- Memory prepare perf samples now carry `source` context (`operator`, `resume`, `prewarm`), and perf artifact selection prefers operator-triggered samples so prewarm runs do not taint interactive completion timings.
- Chat message memory actions now render inline instead of behind a disclosure toggle; the E2E coverage accepts both inline and legacy tray access paths during the transition.
