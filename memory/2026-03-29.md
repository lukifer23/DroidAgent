# 2026-03-29

## Durable host state

- The owner passkey is enrolled on the Mac host.
- The canonical daily-use remote URL is `https://mac.taila06290.ts.net`.
- DroidAgent runs as a LaunchAgent-backed local service on this Mac.
- The default local runtime path is Ollama with `qwen3.5:4b` at a `65k` context budget.
- Local multimodal chat attachments reuse the selected primary model when Ollama reports `vision`; `qwen2.5vl:3b` remains the fallback for text-only primaries.
- Local semantic memory uses `embeddinggemma:300m-qat-q8_0`.

## Product direction

- Keep DroidAgent local-first, mobile-first, and Tailscale-first.
- Prioritize low latency, readable UI, real OpenClaw sessions, and clear live feedback.
- Avoid placeholders, stub flows, duplicate settings, and silent failures.
- Make the operator experience as close to one-click as the host and security model allow.
